<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="index.css" />
    <title>to-do app</title>
  </head>
  <body>
    <div class="box" id="heading">
      <h1><%= listTitle%></h1>
    </div>
    <div class="box">
      <% if(inputValue){ %> <% inputValue.forEach((val)=>{%>
      <div class="item">
        <input type="checkbox" />
        <p><%= val %></p>
      </div>
      <% }) %> <% } %>
      <form class="item" action="/" method="post">
        <input
          type="text"
          name="task"
          placeholder="New Item"
          autocomplete="off"
          required
        />
        <button class="rotate" type="submit" name="list" value="<%=listTitle%>">
          +
        </button>
      </form>

      <% if(listTitle == "Work-List" ){ %>
      <a href="/"> Go to Main-list</a>
      <% } else { %>
      <a href="/work"> Go to work-list</a>
      <% } %>
    </div>
  </body>
</html>
